components:
  schemas:
    ConsistencyGroup_v1_0_0_Actions:
      additionalProperties: false
      description: The available actions for this resource.
      properties:
        '#ConsistencyGroup.AssignReplicaTarget':
          $ref: '#/components/schemas/ConsistencyGroup_v1_0_0_AssignReplicaTarget'
        '#ConsistencyGroup.CreateReplicaTarget':
          $ref: '#/components/schemas/ConsistencyGroup_v1_0_0_CreateReplicaTarget'
        '#ConsistencyGroup.RemoveReplicaRelationship':
          $ref: '#/components/schemas/ConsistencyGroup_v1_0_0_RemoveReplicaRelationship'
        '#ConsistencyGroup.ResumeReplication':
          $ref: '#/components/schemas/ConsistencyGroup_v1_0_0_ResumeReplication'
        '#ConsistencyGroup.ReverseReplicationRelationship':
          $ref: '#/components/schemas/ConsistencyGroup_v1_0_0_ReverseReplicationRelationship'
        '#ConsistencyGroup.SplitReplication':
          $ref: '#/components/schemas/ConsistencyGroup_v1_0_0_SplitReplication'
        '#ConsistencyGroup.SuspendReplication':
          $ref: '#/components/schemas/ConsistencyGroup_v1_0_0_SuspendReplication'
        Oem:
          $ref: '#/components/schemas/ConsistencyGroup_v1_0_0_OemActions'
          description: This property contains the available OEM specific actions for
            this resource.
          x-longDescription: This property shall contain any additional OEM actions
            for this resource.
      type: object
      x-longDescription: This property shall contain any actions for this resource.
      x-patternProperties:
        ^([a-zA-Z_][a-zA-Z0-9_]*)?@(odata|Redfish|Message)\.[a-zA-Z_][a-zA-Z0-9_]*$:
          description: This property shall specify a valid odata or Redfish property.
    ConsistencyGroup_v1_0_0_AssignReplicaTarget:
      additionalProperties: false
      description: This action is used to establish a replication relationship by
        assigning an existing consistency group to serve as a target replica for an
        existing source consistency group.
      properties:
        target:
          description: Link to invoke action
          format: uri-reference
          type: string
        title:
          description: Friendly action name
          type: string
      type: object
      x-longDescription: This action shall be used to establish a replication relationship
        by assigning an existing consistency group to serve as a target replica for
        an existing source consistency group.
      x-patternProperties:
        ^([a-zA-Z_][a-zA-Z0-9_]*)?@(odata|Redfish|Message)\.[a-zA-Z_][a-zA-Z0-9_]*$:
          description: This property shall specify a valid odata or Redfish property.
    ConsistencyGroup_v1_0_0_AssignReplicaTargetRequestBody:
      additionalProperties: false
      description: This action is used to establish a replication relationship by
        assigning an existing consistency group to serve as a target replica for an
        existing source consistency group.
      properties:
        ReplicaType:
          $ref: http://redfish.dmtf.org/schemas/swordfish/v1/StorageReplicaInfo.yaml#/components/schemas/StorageReplicaInfo_ReplicaType
          description: The type of replica relationship to be created (e.g., Clone,
            Mirror, Snap).
          x-longDescription: This parameter shall contain the type of replica relationship
            to be created.
        ReplicaUpdateMode:
          $ref: http://redfish.dmtf.org/schemas/swordfish/v1/StorageReplicaInfo.yaml#/components/schemas/StorageReplicaInfo_ReplicaUpdateMode
          description: The replica update mode (synchronous vs asynchronous).
          x-longDescription: This parameter shall specify the replica update mode.
        TargetConsistencyGroup:
          description: The Uri to the existing target consistency group.
          type: string
          x-longDescription: This parameter shall contain the Uri to the existing
            consistency group.
      required:
      - ReplicaType
      - ReplicaUpdateMode
      - TargetConsistencyGroup
      type: object
      x-longDescription: This action shall be used to establish a replication relationship
        by assigning an existing consistency group to serve as a target replica for
        an existing source consistency group.
      x-patternProperties:
        ^([a-zA-Z_][a-zA-Z0-9_]*)?@(odata|Redfish|Message)\.[a-zA-Z_][a-zA-Z0-9_]*$:
          description: This property shall specify a valid odata or Redfish property.
    ConsistencyGroup_v1_0_0_ConsistencyGroup:
      additionalProperties: false
      description: A collection of volumes grouped together to ensure write order
        consistency across all those volumes.
      properties:
        '@odata.context':
          $ref: http://redfish.dmtf.org/schemas/v1/odata-v4.yaml#/components/schemas/odata-v4_context
        '@odata.etag':
          $ref: http://redfish.dmtf.org/schemas/v1/odata-v4.yaml#/components/schemas/odata-v4_etag
        '@odata.id':
          $ref: http://redfish.dmtf.org/schemas/v1/odata-v4.yaml#/components/schemas/odata-v4_id
        '@odata.type':
          $ref: http://redfish.dmtf.org/schemas/v1/odata-v4.yaml#/components/schemas/odata-v4_type
        Actions:
          $ref: '#/components/schemas/ConsistencyGroup_v1_0_0_Actions'
          description: The available actions for this resource.
          x-longDescription: The Actions property shall contain the available actions
            for this resource.
        ConsistencyMethod:
          description: The consistency method used by this group.
          oneOf:
          - $ref: http://redfish.dmtf.org/schemas/swordfish/v1/ConsistencyGroup.yaml#/components/schemas/ConsistencyGroup_ApplicationConsistencyMethod
          - enum:
            - null
          readOnly: false
          x-longDescription: The property shall set the consistency method used by
            this group.
        ConsistencyType:
          description: The consistency type used by this group.
          oneOf:
          - $ref: http://redfish.dmtf.org/schemas/swordfish/v1/ConsistencyGroup.yaml#/components/schemas/ConsistencyGroup_ConsistencyType
          - enum:
            - null
          readOnly: false
          x-longDescription: This property shall set the consistency type used by
            this group.
        Description:
          oneOf:
          - $ref: http://redfish.dmtf.org/schemas/v1/Resource.yaml#/components/schemas/Resource_Description
          - enum:
            - null
          readOnly: true
        Id:
          $ref: http://redfish.dmtf.org/schemas/v1/Resource.yaml#/components/schemas/Resource_Id
          readOnly: true
        IsConsistent:
          description: This value is true when the consistency group is in a consistent
            state.
          nullable: true
          readOnly: true
          type: boolean
          x-longDescription: The value of this property shall be set to true when
            the consistency group is in a consistent state.
        Links:
          $ref: '#/components/schemas/ConsistencyGroup_v1_0_0_Links'
          description: Contains links to other resources that are related to this
            resource.
          x-longDescription: This property shall contain links to other resources
            that are related to this resource.
        Name:
          $ref: http://redfish.dmtf.org/schemas/v1/Resource.yaml#/components/schemas/Resource_Name
          readOnly: true
        Oem:
          $ref: http://redfish.dmtf.org/schemas/v1/Resource.yaml#/components/schemas/Resource_Oem
          description: The OEM extension property.
          x-longDescription: This property shall contain the OEM extensions.  All
            values for properties that this object contains shall conform to the Redfish
            Specification-described requirements.
        ReplicaInfo:
          $ref: http://redfish.dmtf.org/schemas/swordfish/v1/StorageReplicaInfo.v1_4_0.yaml#/components/schemas/StorageReplicaInfo_v1_4_0_ReplicaInfo
          description: Describes this storage group in its role as a target for replication.
          x-longDescription: This property shall describe the replication relationship
            between this storage group and a corresponding source storage group.
        ReplicaTargets:
          description: The resources that are target replicas of this source.
          items:
            $ref: http://redfish.dmtf.org/schemas/v1/odata-v4.yaml#/components/schemas/odata-v4_idRef
          readOnly: true
          type: array
          x-longDescription: The value shall reference the target replicas that are
            sourced by this replica.
        ReplicaTargets@odata.count:
          $ref: http://redfish.dmtf.org/schemas/v1/odata-v4.yaml#/components/schemas/odata-v4_count
        Status:
          $ref: http://redfish.dmtf.org/schemas/v1/Resource.yaml#/components/schemas/Resource_Status
          description: The property contains the status of the ConsistencyGroup.
          x-longDescription: The property shall contain the status of the ConsistencyGroup.
        Volumes:
          description: Volumes in this storage group.
          items:
            $ref: http://redfish.dmtf.org/schemas/v1/odata-v4.yaml#/components/schemas/odata-v4_idRef
          readOnly: false
          type: array
          x-longDescription: An array of references to volumes managed by this storage
            group.
        Volumes@odata.count:
          $ref: http://redfish.dmtf.org/schemas/v1/odata-v4.yaml#/components/schemas/odata-v4_count
      required:
      - '@odata.id'
      - '@odata.type'
      - Id
      - Name
      type: object
      x-longDescription: A collection of volumes grouped together to ensure write
        order consistency across all those volumes. A management operation on a consistency
        group, such as configuring replication properties, applies to all the volumes
        within the consistency group.
      x-patternProperties:
        ^([a-zA-Z_][a-zA-Z0-9_]*)?@(odata|Redfish|Message)\.[a-zA-Z_][a-zA-Z0-9_]*$:
          description: This property shall specify a valid odata or Redfish property.
    ConsistencyGroup_v1_0_0_CreateReplicaTarget:
      additionalProperties: false
      description: This action is used to create a new consistency group resource
        to provide expanded data protection through a replica relationship with the
        specified source consistency group.
      properties:
        target:
          description: Link to invoke action
          format: uri-reference
          type: string
        title:
          description: Friendly action name
          type: string
      type: object
      x-longDescription: This action shall be used to create a new consistency group
        resource to provide expanded data protection through a replica relationship
        with the specified source consistency group.
      x-patternProperties:
        ^([a-zA-Z_][a-zA-Z0-9_]*)?@(odata|Redfish|Message)\.[a-zA-Z_][a-zA-Z0-9_]*$:
          description: This property shall specify a valid odata or Redfish property.
    ConsistencyGroup_v1_0_0_CreateReplicaTargetRequestBody:
      additionalProperties: false
      description: This action is used to create a new consistency group resource
        to provide expanded data protection through a replica relationship with the
        specified source consistency group.
      properties:
        ConsistencyGroupName:
          description: The Name for the new target consistency group.
          type: string
          x-longDescription: This parameter shall contain the Name for the target
            consistency group.
        ReplicaType:
          $ref: http://redfish.dmtf.org/schemas/swordfish/v1/StorageReplicaInfo.yaml#/components/schemas/StorageReplicaInfo_ReplicaType
          description: The type of replica relationship to be created (e.g., Clone,
            Mirror, Snap).
          x-longDescription: This parameter shall contain the type of replica relationship
            to be created.
        ReplicaUpdateMode:
          $ref: http://redfish.dmtf.org/schemas/swordfish/v1/StorageReplicaInfo.yaml#/components/schemas/StorageReplicaInfo_ReplicaUpdateMode
          description: The replica update mode (synchronous vs asynchronous).
          x-longDescription: This parameter shall specify the replica update mode.
        TargetStoragePool:
          description: The Uri to the existing target Storage Pool.
          type: string
          x-longDescription: This parameter shall contain the Uri to the existing
            StoragePool in which to create the target consistency group.
      required:
      - ConsistencyGroupName
      - ReplicaType
      - ReplicaUpdateMode
      - TargetStoragePool
      type: object
      x-longDescription: This action shall be used to create a new consistency group
        resource to provide expanded data protection through a replica relationship
        with the specified source consistency group.
      x-patternProperties:
        ^([a-zA-Z_][a-zA-Z0-9_]*)?@(odata|Redfish|Message)\.[a-zA-Z_][a-zA-Z0-9_]*$:
          description: This property shall specify a valid odata or Redfish property.
    ConsistencyGroup_v1_0_0_Links:
      additionalProperties: false
      description: Contains links to other resources that are related to this resource.
      properties:
        Oem:
          $ref: http://redfish.dmtf.org/schemas/v1/Resource.yaml#/components/schemas/Resource_Oem
          description: The OEM extension property.
          x-longDescription: This property shall contain the OEM extensions.  All
            values for properties contained in this object shall conform to the Redfish
            Specification-described requirements.
      type: object
      x-longDescription: This structure shall contain references to resources that
        are not contained within this resource.
      x-patternProperties:
        ^([a-zA-Z_][a-zA-Z0-9_]*)?@(odata|Redfish|Message)\.[a-zA-Z_][a-zA-Z0-9_]*$:
          description: This property shall specify a valid odata or Redfish property.
    ConsistencyGroup_v1_0_0_OemActions:
      additionalProperties: true
      description: The available OEM specific actions for this resource.
      properties: {}
      type: object
      x-longDescription: This type shall contain any additional OEM actions for this
        resource.
      x-patternProperties:
        ^([a-zA-Z_][a-zA-Z0-9_]*)?@(odata|Redfish|Message)\.[a-zA-Z_][a-zA-Z0-9_]*$:
          description: This property shall specify a valid odata or Redfish property.
    ConsistencyGroup_v1_0_0_RemoveReplicaRelationship:
      additionalProperties: false
      description: This action is used to disable data synchronization between a source
        and target consistency group, remove the replication relationship, and optionally
        delete the target consistency group.
      properties:
        target:
          description: Link to invoke action
          format: uri-reference
          type: string
        title:
          description: Friendly action name
          type: string
      type: object
      x-longDescription: This action shall be used to disable data synchronization
        between a source and target consistency group, remove the replication relationship,
        and optionally delete the target consistency group.
      x-patternProperties:
        ^([a-zA-Z_][a-zA-Z0-9_]*)?@(odata|Redfish|Message)\.[a-zA-Z_][a-zA-Z0-9_]*$:
          description: This property shall specify a valid odata or Redfish property.
    ConsistencyGroup_v1_0_0_RemoveReplicaRelationshipRequestBody:
      additionalProperties: false
      description: This action is used to disable data synchronization between a source
        and target consistency group, remove the replication relationship, and optionally
        delete the target consistency group.
      properties:
        DeleteTargetConsistencyGroup:
          description: Indicate whether or not to delete the target consistency group
            as part of the operation.
          type: boolean
          x-longDescription: This parameter shall indicate whether or not to delete
            the target consistency group as part of the operation. If not specified,
            the system should use its default behavior.
        TargetConsistencyGroup:
          description: The Uri to the existing target consistency group.
          type: string
          x-longDescription: This parameter shall contain the Uri to the existing
            target consistency group.
      required:
      - TargetConsistencyGroup
      type: object
      x-longDescription: This action shall be used to disable data synchronization
        between a source and target consistency group, remove the replication relationship,
        and optionally delete the target consistency group.
      x-patternProperties:
        ^([a-zA-Z_][a-zA-Z0-9_]*)?@(odata|Redfish|Message)\.[a-zA-Z_][a-zA-Z0-9_]*$:
          description: This property shall specify a valid odata or Redfish property.
    ConsistencyGroup_v1_0_0_ResumeReplication:
      additionalProperties: false
      description: This action is used to resume the active data synchronization between
        a source and target consistency group, without otherwise altering the replication
        relationship.
      properties:
        target:
          description: Link to invoke action
          format: uri-reference
          type: string
        title:
          description: Friendly action name
          type: string
      type: object
      x-longDescription: This action shall be used to resume the active data synchronization
        between a source and target consistency group, without otherwise altering
        the replication relationship.
      x-patternProperties:
        ^([a-zA-Z_][a-zA-Z0-9_]*)?@(odata|Redfish|Message)\.[a-zA-Z_][a-zA-Z0-9_]*$:
          description: This property shall specify a valid odata or Redfish property.
    ConsistencyGroup_v1_0_0_ResumeReplicationRequestBody:
      additionalProperties: false
      description: This action is used to resume the active data synchronization between
        a source and target consistency group, without otherwise altering the replication
        relationship.
      properties:
        TargetConsistencyGroup:
          description: The Uri to the existing target consistency group.
          type: string
          x-longDescription: This parameter shall contain the Uri to the existing
            target consistency group.
      required:
      - TargetConsistencyGroup
      type: object
      x-longDescription: This action shall be used to resume the active data synchronization
        between a source and target consistency group, without otherwise altering
        the replication relationship.
      x-patternProperties:
        ^([a-zA-Z_][a-zA-Z0-9_]*)?@(odata|Redfish|Message)\.[a-zA-Z_][a-zA-Z0-9_]*$:
          description: This property shall specify a valid odata or Redfish property.
    ConsistencyGroup_v1_0_0_ReverseReplicationRelationship:
      additionalProperties: false
      description: This action is used to reverse the replication relationship between
        a source and target consistency group.
      properties:
        target:
          description: Link to invoke action
          format: uri-reference
          type: string
        title:
          description: Friendly action name
          type: string
      type: object
      x-longDescription: This action shall be used to reverse the replication relationship
        between a source and target consistency group.
      x-patternProperties:
        ^([a-zA-Z_][a-zA-Z0-9_]*)?@(odata|Redfish|Message)\.[a-zA-Z_][a-zA-Z0-9_]*$:
          description: This property shall specify a valid odata or Redfish property.
    ConsistencyGroup_v1_0_0_ReverseReplicationRelationshipRequestBody:
      additionalProperties: false
      description: This action is used to reverse the replication relationship between
        a source and target consistency group.
      properties:
        TargetConsistencyGroup:
          description: The Uri to the existing target consistency group.
          type: string
          x-longDescription: This parameter shall contain the Uri to the existing
            target consistency group.
      required:
      - TargetConsistencyGroup
      type: object
      x-longDescription: This action shall be used to reverse the replication relationship
        between a source and target consistency group.
      x-patternProperties:
        ^([a-zA-Z_][a-zA-Z0-9_]*)?@(odata|Redfish|Message)\.[a-zA-Z_][a-zA-Z0-9_]*$:
          description: This property shall specify a valid odata or Redfish property.
    ConsistencyGroup_v1_0_0_SplitReplication:
      additionalProperties: false
      description: This action is used to split the replication relationship and suspend
        data synchronization between a source and target consistency group.
      properties:
        target:
          description: Link to invoke action
          format: uri-reference
          type: string
        title:
          description: Friendly action name
          type: string
      type: object
      x-longDescription: This action shall be used to split the replication relationship
        and suspend data synchronization between a source and target consistency group.
      x-patternProperties:
        ^([a-zA-Z_][a-zA-Z0-9_]*)?@(odata|Redfish|Message)\.[a-zA-Z_][a-zA-Z0-9_]*$:
          description: This property shall specify a valid odata or Redfish property.
    ConsistencyGroup_v1_0_0_SplitReplicationRequestBody:
      additionalProperties: false
      description: This action is used to split the replication relationship and suspend
        data synchronization between a source and target consistency group.
      properties:
        TargetConsistencyGroup:
          description: The Uri to the existing target consistency group.
          type: string
          x-longDescription: This parameter shall contain the Uri to the existing
            target consistency group.
      required:
      - TargetConsistencyGroup
      type: object
      x-longDescription: This action shall be used to split the replication relationship
        and suspend data synchronization between a source and target consistency group.
      x-patternProperties:
        ^([a-zA-Z_][a-zA-Z0-9_]*)?@(odata|Redfish|Message)\.[a-zA-Z_][a-zA-Z0-9_]*$:
          description: This property shall specify a valid odata or Redfish property.
    ConsistencyGroup_v1_0_0_SuspendReplication:
      additionalProperties: false
      description: This action is used to suspend active data synchronization between
        a source and target consistency group, without otherwise altering the replication
        relationship.
      properties:
        target:
          description: Link to invoke action
          format: uri-reference
          type: string
        title:
          description: Friendly action name
          type: string
      type: object
      x-longDescription: This action shall be used to suspend active data synchronization
        between a source and target consistency group, without otherwise altering
        the replication relationship.
      x-patternProperties:
        ^([a-zA-Z_][a-zA-Z0-9_]*)?@(odata|Redfish|Message)\.[a-zA-Z_][a-zA-Z0-9_]*$:
          description: This property shall specify a valid odata or Redfish property.
    ConsistencyGroup_v1_0_0_SuspendReplicationRequestBody:
      additionalProperties: false
      description: This action is used to suspend active data synchronization between
        a source and target consistency group, without otherwise altering the replication
        relationship.
      properties:
        TargetConsistencyGroup:
          description: The Uri to the existing target consistency group.
          type: string
          x-longDescription: This parameter shall contain the Uri to the existing
            target consistency group.
      required:
      - TargetConsistencyGroup
      type: object
      x-longDescription: This action shall be used to suspend active data synchronization
        between a source and target consistency group, without otherwise altering
        the replication relationship.
      x-patternProperties:
        ^([a-zA-Z_][a-zA-Z0-9_]*)?@(odata|Redfish|Message)\.[a-zA-Z_][a-zA-Z0-9_]*$:
          description: This property shall specify a valid odata or Redfish property.
title: '#ConsistencyGroup.v1_0_0.ConsistencyGroup'
x-copyright: Copyright 2015-2026 Storage Networking Industry Association (SNIA), USA.
  All rights reserved.
x-language: en
x-owningEntity: SNIA
x-release: 1.1.0
