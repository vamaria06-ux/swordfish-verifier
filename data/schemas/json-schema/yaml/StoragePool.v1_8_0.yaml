components:
  schemas:
    StoragePool_v1_8_0_Actions:
      additionalProperties: false
      description: The available actions for this resource.
      properties:
        '#StoragePool.AddDrives':
          $ref: '#/components/schemas/StoragePool_v1_8_0_AddDrives'
        '#StoragePool.RemoveDrives':
          $ref: '#/components/schemas/StoragePool_v1_8_0_RemoveDrives'
        '#StoragePool.SetCompressionState':
          $ref: '#/components/schemas/StoragePool_v1_8_0_SetCompressionState'
        '#StoragePool.SetDeduplicationState':
          $ref: '#/components/schemas/StoragePool_v1_8_0_SetDeduplicationState'
        '#StoragePool.SetEncryptionState':
          $ref: '#/components/schemas/StoragePool_v1_8_0_SetEncryptionState'
        Oem:
          $ref: '#/components/schemas/StoragePool_v1_8_0_OemActions'
          description: This property contains the available OEM specific actions for
            this resource.
          x-longDescription: This property shall contain any additional OEM actions
            for this resource.
          x-versionAdded: v1_3_0
      type: object
      x-longDescription: The Actions property shall contain the available actions
        for this resource.
      x-patternProperties:
        ^([a-zA-Z_][a-zA-Z0-9_]*)?@(odata|Redfish|Message)\.[a-zA-Z_][a-zA-Z0-9_]*$:
          description: This property shall specify a valid odata or Redfish property.
    StoragePool_v1_8_0_AddDrives:
      additionalProperties: false
      description: This action is used to add an additional drive, or set of drives,
        to a capacity source for the storage pool.
      properties:
        target:
          description: Link to invoke action
          format: uri-reference
          type: string
        title:
          description: Friendly action name
          type: string
      type: object
      x-longDescription: This action shall be used to add a drive, or set of drives,
        to an underlying capacity source for the storage pool.
      x-patternProperties:
        ^([a-zA-Z_][a-zA-Z0-9_]*)?@(odata|Redfish|Message)\.[a-zA-Z_][a-zA-Z0-9_]*$:
          description: This property shall specify a valid odata or Redfish property.
    StoragePool_v1_8_0_AddDrivesRequestBody:
      additionalProperties: false
      description: This action is used to add an additional drive, or set of drives,
        to a capacity source for the storage pool.
      properties:
        CapacitySource:
          $ref: http://redfish.dmtf.org/schemas/swordfish/v1/Capacity.yaml#/components/schemas/Capacity_CapacitySource
          description: The capacity source to be extended.
          x-longDescription: This parameter shall contain the target capacity source
            for the drive(s). This property does not need to be specified if the storage
            pool only contains one capacity source, or if the implementation is capable
            of automatically selecting the appropriate capacity source.
        Drives:
          description: The drive(s) to be added.
          items:
            $ref: http://redfish.dmtf.org/schemas/v1/odata-v4.yaml#/components/schemas/odata-v4_idRef
          type: array
          x-longDescription: This parameter shall contain the Uri to the existing
            drive or drives to be added to a capacity source of the storage pool.
            The implementation may impose restrictions on the number of drives added
            simultaneously.
      required:
      - Drives
      type: object
      x-longDescription: This action shall be used to add a drive, or set of drives,
        to an underlying capacity source for the storage pool.
      x-patternProperties:
        ^([a-zA-Z_][a-zA-Z0-9_]*)?@(odata|Redfish|Message)\.[a-zA-Z_][a-zA-Z0-9_]*$:
          description: This property shall specify a valid odata or Redfish property.
    StoragePool_v1_8_0_EndGrpLifetime:
      additionalProperties: false
      description: This contains properties the Endurance Group Lifetime attributes.
      properties:
        DataUnitsRead:
          description: The property contains the total number of data units read from
            this endurance group.
          format: int64
          minimum: 0
          nullable: true
          readOnly: true
          type: integer
          x-longDescription: The property shall contain the total number of data units
            read from this endurance group. This value does not include controller
            reads due to internal operations such as garbage collection. The value
            is reported in billions, where a value of 1 corresponds to 1 billion bytes
            written, and is rounded up. A value of zero indicates the property is
            unsupported.
          x-versionAdded: v1_4_0
        DataUnitsWritten:
          description: The property contains the total number of data units written
            from this endurance group.
          format: int64
          minimum: 0
          nullable: true
          readOnly: true
          type: integer
          x-longDescription: The property shall contain the total number of data units
            written from this endurance group. This value does not include controller
            writes due to internal operations such as garbage collection. The value
            is reported in billions, where a value of 1 corresponds to 1 billion bytes
            written, and is rounded up. A value of zero indicates the property is
            unsupported.
          x-versionAdded: v1_4_0
        EnduranceEstimate:
          description: This property contains an estimate of the total number of data
            bytes that may be written to the Endurance Group over the lifetime of
            the Endurance Group assuming a write amplication of 1.
          format: int64
          minimum: 0
          nullable: true
          readOnly: true
          type: integer
          x-longDescription: This property shall contain an estimate of the total
            number of data bytes that may be written to the Endurance Group over the
            lifetime of the Endurance Group assuming a write amplication of 1.  The
            value is reported in billions, where a value of 1 corresponds to 1 billion
            bytes written, and is rounded up. A value of zero indicates endurance
            estimates are unsupported.
          x-versionAdded: v1_4_0
        ErrorInformationLogEntryCount:
          description: This property contains the number of error information log
            entries over the life of the controller for the endurance group.
          format: int64
          minimum: 0
          nullable: true
          readOnly: true
          type: integer
          x-longDescription: This property shall contain the number of error information
            log entries over the life of the controller for the endurance group.
          x-versionAdded: v1_4_0
        HostReadCommandCount:
          description: This property contains the number of read commands completed
            by all controllers in the NVM subsystem for the Endurance Group.
          format: int64
          minimum: 0
          nullable: true
          readOnly: true
          type: integer
          x-longDescription: This property shall contain the number of read commands
            completed by all controllers in the NVM subsystem for the Endurance Group.
            For the NVM command set, the is the number of compare commands and read
            commands.
          x-versionAdded: v1_4_0
        HostWriteCommandCount:
          description: This property contains the number of write commands completed
            by all controllers in the NVM subsystem for the Endurance Group.
          format: int64
          minimum: 0
          nullable: true
          readOnly: true
          type: integer
          x-longDescription: This property shall contain the number of write commands
            completed by all controllers in the NVM subsystem for the Endurance Group.
            For the NVM command set, the is the number of compare commands and write
            commands.
          x-versionAdded: v1_4_0
        MediaAndDataIntegrityErrorCount:
          description: This property contains the number of occurences where the controller
            detected an unrecovered data integrity error for the Endurance Group.
          format: int64
          minimum: 0
          nullable: true
          readOnly: true
          type: integer
          x-longDescription: This property shall contain the number of occurences
            where the controller detected an unrecovered data integrity error for
            the Endurance Group. Errors such as uncorrectable ECC, CRC checksum failure,
            or LBA tag mismatch are included in this field.
          x-versionAdded: v1_4_0
        MediaUnitsWritten:
          description: The property contains the total number of data units written
            from this endurance group.
          format: int64
          minimum: 0
          nullable: true
          readOnly: true
          type: integer
          x-longDescription: The property shall contain the total number of data units
            written from this endurance group. This value includes host and controller
            writes due to internal operations such as garbage collection. The value
            is reported in billions, where a value of 1 corresponds to 1 billion bytes
            written, and is rounded up. A value of zero indicates the property is
            unsupported.
          x-versionAdded: v1_4_0
        PercentUsed:
          description: A vendor-specific estimate of the percent life used for the
            endurance group based on the actual usage and the manufacturer prediction
            of NVM life.
          format: int64
          minimum: 0
          nullable: true
          readOnly: true
          type: integer
          x-longDescription: This property shall contain a vendor-specific estimate
            of the percent life used for the endurance group based on the actual usage
            and the manufacturer prediction of NVM life. A value of 100 indicates
            that the estimated endurance of the NVM in the Endurance Group has been
            consumed, but may not indicate an NVM failure. According to the NVMe and
            JEDEC specs, the value is allowed to exceed 100. Percentages greater than
            254 shall be represented as 255.
          x-versionAdded: v1_4_0
      type: object
      x-longDescription: This contains properties for the Endurance Group Lifetime
        attributes.
      x-patternProperties:
        ^([a-zA-Z_][a-zA-Z0-9_]*)?@(odata|Redfish|Message)\.[a-zA-Z_][a-zA-Z0-9_]*$:
          description: This property shall specify a valid odata or Redfish property.
    StoragePool_v1_8_0_Links:
      additionalProperties: false
      description: The links object contains the links to other resources that are
        related to this resource.
      properties:
        DedicatedSpareDrives:
          description: An array of references to the drives which are dedicated spares
            for this StoragePool.
          items:
            $ref: http://redfish.dmtf.org/schemas/v1/odata-v4.yaml#/components/schemas/odata-v4_idRef
          readOnly: true
          type: array
          x-longDescription: The value of this property shall be a reference to the
            resources that this StoragePool is associated with and shall reference
            resources of type Drive. This property shall only contain references to
            Drive entities which are currently assigned as a dedicated spare and are
            able to support this StoragePool.
          x-versionAdded: v1_2_0
        DedicatedSpareDrives@odata.count:
          $ref: http://redfish.dmtf.org/schemas/v1/odata-v4.yaml#/components/schemas/odata-v4_count
        DefaultClassOfService:
          $ref: http://redfish.dmtf.org/schemas/v1/odata-v4.yaml#/components/schemas/odata-v4_idRef
          deprecated: true
          description: The default class of service for entities allocated from this
            storage pool.
          readOnly: false
          x-deprecatedReason: Default ClassOfService moved from Links to NavigationProperty.
          x-longDescription: If present, this property shall reference the default
            class of service for entities allocated from this storage pool.  If the
            ClassesOfService collection is not empty, then the value of this property
            shall be one of its entries. If not present, the default class of service
            of the containing StorageService entity shall be used.
        Oem:
          $ref: http://redfish.dmtf.org/schemas/v1/Resource.yaml#/components/schemas/Resource_Oem
          description: The OEM extension property.
          x-longDescription: This property shall contain the OEM extensions.  All
            values for properties contained in this object shall conform to the Redfish
            Specification-described requirements.
        OwningStorageResource:
          $ref: http://redfish.dmtf.org/schemas/v1/odata-v4.yaml#/components/schemas/odata-v4_idRef
          description: A pointer to the Storage resource that owns or contains this
            StoragePool.
          readOnly: true
          x-longDescription: This shall be a pointer to the Storage resource that
            owns or contains this StoragePool.
          x-versionAdded: v1_4_0
        SpareResourceSets:
          description: An array of references to SpareResourceSets.
          items:
            $ref: http://redfish.dmtf.org/schemas/v1/odata-v4.yaml#/components/schemas/odata-v4_idRef
          readOnly: false
          type: array
          x-longDescription: Each referenced SpareResourceSet shall contain resources
            that may be utilized to replace the capacity provided by a failed resource
            having a compatible type.
          x-versionAdded: v1_2_0
        SpareResourceSets@odata.count:
          $ref: http://redfish.dmtf.org/schemas/v1/odata-v4.yaml#/components/schemas/odata-v4_count
      type: object
      x-longDescription: The Links property, as described by the Redfish Specification,
        shall contain references to resources that are related to, but not contained
        by (subordinate to), this resource.
      x-patternProperties:
        ^([a-zA-Z_][a-zA-Z0-9_]*)?@(odata|Redfish|Message)\.[a-zA-Z_][a-zA-Z0-9_]*$:
          description: This property shall specify a valid odata or Redfish property.
    StoragePool_v1_8_0_NVMeEnduranceGroupProperties:
      additionalProperties: false
      description: This contains properties to use when StoragePool is used to describe
        an NVMe Endurance Group.
      properties:
        EndGrpLifetime:
          description: This property contains the Endurance Group Lifetime properties.
          oneOf:
          - $ref: '#/components/schemas/StoragePool_v1_8_0_EndGrpLifetime'
          - enum:
            - null
          x-longDescription: This property shall contain any Endurance Group Lifetime
            properties.
          x-versionAdded: v1_4_0
        PredictedMediaLifeLeftPercent:
          description: The percentage of reads and writes that are predicted to be
            available for the media.
          nullable: true
          readOnly: true
          type: number
          x-longDescription: This property shall contain an indicator of the percentage
            of life remaining in the drive's media.
          x-units: '%'
          x-versionAdded: v1_4_0
      type: object
      x-longDescription: This contains properties to use when StoragePool is used
        to describe an NVMe Endurance Group.
      x-patternProperties:
        ^([a-zA-Z_][a-zA-Z0-9_]*)?@(odata|Redfish|Message)\.[a-zA-Z_][a-zA-Z0-9_]*$:
          description: This property shall specify a valid odata or Redfish property.
    StoragePool_v1_8_0_NVMeProperties:
      additionalProperties: false
      description: This contains properties to use when StoragePool is used to describe
        an NVMe construct.
      properties:
        NVMePoolType:
          description: Indicates whether the StoragePool is used as an EnduranceGroup
            or an NVMSet.
          oneOf:
          - $ref: http://redfish.dmtf.org/schemas/swordfish/v1/StoragePool.yaml#/components/schemas/StoragePool_NVMePoolType
          - enum:
            - null
          readOnly: true
          x-longDescription: This property shall indicate whether the StoragePool
            is used as an EnduranceGroup or an NVMSet.
          x-versionAdded: v1_6_0
      type: object
      x-longDescription: This contains properties to use when StoragePool is used
        to describe an NVMe construct.
      x-patternProperties:
        ^([a-zA-Z_][a-zA-Z0-9_]*)?@(odata|Redfish|Message)\.[a-zA-Z_][a-zA-Z0-9_]*$:
          description: This property shall specify a valid odata or Redfish property.
    StoragePool_v1_8_0_NVMeSetProperties:
      additionalProperties: false
      description: This contains properties to use when StoragePool is used to describe
        an NVMe Set.
      properties:
        EnduranceGroupIdentifier:
          description: A 16-bit hex value that contains the endurance group identifier.
          nullable: true
          pattern: ^0[xX](([a-fA-F]|[0-9])*)$
          readOnly: true
          type: string
          x-longDescription: This property shall contain a 16-bit hex value that contains
            the endurance group identifier.  The endurance group identifier is unique
            within a subsystem.  Reserved values include 0.
          x-versionAdded: v1_4_0
        OptimalWriteSizeBytes:
          description: This property contains the Optimal Write Size in Bytes for
            this NVMe Set.
          format: int64
          minimum: 0
          nullable: true
          readOnly: true
          type: integer
          x-longDescription: This property shall contain the Optimal Write Size in
            Bytes for this NVMe Set.
          x-units: By
          x-versionAdded: v1_4_0
        Random4kReadTypicalNanoSeconds:
          description: Indicates the typical time to complete a 4k read in 100 nano-second
            units when the NVM Set is in a Predictable Latency Mode Deterministic
            Window and there is 1 outstanding command per NVM Set.
          format: int64
          minimum: 0
          nullable: true
          readOnly: true
          type: integer
          x-longDescription: This property shall contain the typical time to complete
            a 4k read in 100 nano-second units when the NVM Set is in a Predictable
            Latency Mode Deterministic Window and there is 1 outstanding command per
            NVM Set.
          x-versionAdded: v1_4_0
        SetIdentifier:
          description: A 16-bit hex value that contains the NVMe Set group identifier.
          nullable: true
          pattern: ^0[xX](([a-fA-F]|[0-9])*)$
          readOnly: true
          type: string
          x-longDescription: This property shall contain a 16-bit hex value that contains
            the NVMe Set group identifier. The NVM Set identifier is unique within
            a subsystem. Reserved values include 0.
          x-versionAdded: v1_4_0
        UnallocatedNVMNamespaceCapacityBytes:
          description: Indicates the unallocated capacity of the NVMe Set in bytes.
          format: int64
          minimum: 0
          nullable: true
          readOnly: true
          type: integer
          x-longDescription: This property shall contain the unallocated capacity
            of the NVMe Set in bytes.
          x-units: By
          x-versionAdded: v1_4_0
      type: object
      x-longDescription: This contains properties to use when StoragePool is used
        to describe an NVMe Set.
      x-patternProperties:
        ^([a-zA-Z_][a-zA-Z0-9_]*)?@(odata|Redfish|Message)\.[a-zA-Z_][a-zA-Z0-9_]*$:
          description: This property shall specify a valid odata or Redfish property.
    StoragePool_v1_8_0_OemActions:
      additionalProperties: true
      description: The available OEM specific actions for this resource.
      properties: {}
      type: object
      x-longDescription: This type shall contain any additional OEM actions for this
        resource.
      x-patternProperties:
        ^([a-zA-Z_][a-zA-Z0-9_]*)?@(odata|Redfish|Message)\.[a-zA-Z_][a-zA-Z0-9_]*$:
          description: This property shall specify a valid odata or Redfish property.
    StoragePool_v1_8_0_RemoveDrives:
      additionalProperties: false
      description: This action is used to remove drive(s) from the capacity source
        for the StoragePool.
      properties:
        target:
          description: Link to invoke action
          format: uri-reference
          type: string
        title:
          description: Friendly action name
          type: string
      type: object
      x-longDescription: This action shall be used to remove a drive from the StoragePool.
        This action is targeted at a graceful drive removal process, such as initiating
        a drive cleanup and data reallocation before drive removal from the pool.
        The implementation may impose restrictions on the number of drives removed
        simultaneously.
      x-patternProperties:
        ^([a-zA-Z_][a-zA-Z0-9_]*)?@(odata|Redfish|Message)\.[a-zA-Z_][a-zA-Z0-9_]*$:
          description: This property shall specify a valid odata or Redfish property.
    StoragePool_v1_8_0_RemoveDrivesRequestBody:
      additionalProperties: false
      description: This action is used to remove drive(s) from the capacity source
        for the StoragePool.
      properties:
        Drives:
          description: The drive(s) to be removed.
          items:
            $ref: http://redfish.dmtf.org/schemas/v1/odata-v4.yaml#/components/schemas/odata-v4_idRef
          type: array
          x-longDescription: This parameter shall contain the Uri to the drive or
            drives to be removed from the underlying capacity source.
      required:
      - Drives
      type: object
      x-longDescription: This action shall be used to remove a drive from the StoragePool.
        This action is targeted at a graceful drive removal process, such as initiating
        a drive cleanup and data reallocation before drive removal from the pool.
        The implementation may impose restrictions on the number of drives removed
        simultaneously.
      x-patternProperties:
        ^([a-zA-Z_][a-zA-Z0-9_]*)?@(odata|Redfish|Message)\.[a-zA-Z_][a-zA-Z0-9_]*$:
          description: This property shall specify a valid odata or Redfish property.
    StoragePool_v1_8_0_SetCompressionState:
      additionalProperties: false
      description: This action is used to set the compression state of the pool.
      properties:
        target:
          description: Link to invoke action
          format: uri-reference
          type: string
        title:
          description: Friendly action name
          type: string
      type: object
      x-longDescription: This action shall be used to set the compression state of
        the storage pool. This may be both a highly impactful, as well as a long running
        operation.
      x-patternProperties:
        ^([a-zA-Z_][a-zA-Z0-9_]*)?@(odata|Redfish|Message)\.[a-zA-Z_][a-zA-Z0-9_]*$:
          description: This property shall specify a valid odata or Redfish property.
    StoragePool_v1_8_0_SetCompressionStateRequestBody:
      additionalProperties: false
      description: This action is used to set the compression state of the pool.
      properties:
        Enable:
          description: This property indicates the desired compression state of the
            storage pool.
          type: boolean
          x-longDescription: This property shall indicate the desired compression
            state of the storage pool.
      required:
      - Enable
      type: object
      x-longDescription: This action shall be used to set the compression state of
        the storage pool. This may be both a highly impactful, as well as a long running
        operation.
      x-patternProperties:
        ^([a-zA-Z_][a-zA-Z0-9_]*)?@(odata|Redfish|Message)\.[a-zA-Z_][a-zA-Z0-9_]*$:
          description: This property shall specify a valid odata or Redfish property.
    StoragePool_v1_8_0_SetDeduplicationState:
      additionalProperties: false
      description: This action is used to set the dedupe state of the pool.
      properties:
        target:
          description: Link to invoke action
          format: uri-reference
          type: string
        title:
          description: Friendly action name
          type: string
      type: object
      x-longDescription: This action shall be used to set the dedupe state of the
        storage pool. This may be both a highly impactful, as well as a long running
        operation.
      x-patternProperties:
        ^([a-zA-Z_][a-zA-Z0-9_]*)?@(odata|Redfish|Message)\.[a-zA-Z_][a-zA-Z0-9_]*$:
          description: This property shall specify a valid odata or Redfish property.
    StoragePool_v1_8_0_SetDeduplicationStateRequestBody:
      additionalProperties: false
      description: This action is used to set the dedupe state of the pool.
      properties:
        Enable:
          description: This property indicates the desired deduplication state of
            the storage pool.
          type: boolean
          x-longDescription: This property shall indicate the desired deduplication
            state of the storage pool.
      required:
      - Enable
      type: object
      x-longDescription: This action shall be used to set the dedupe state of the
        storage pool. This may be both a highly impactful, as well as a long running
        operation.
      x-patternProperties:
        ^([a-zA-Z_][a-zA-Z0-9_]*)?@(odata|Redfish|Message)\.[a-zA-Z_][a-zA-Z0-9_]*$:
          description: This property shall specify a valid odata or Redfish property.
    StoragePool_v1_8_0_SetEncryptionState:
      additionalProperties: false
      description: This action is used to set the encryption state of the pool.
      properties:
        target:
          description: Link to invoke action
          format: uri-reference
          type: string
        title:
          description: Friendly action name
          type: string
      type: object
      x-longDescription: This action shall be used to set the encryption state of
        the storage pool. This may be both a highly impactful, as well as a long running
        operation.
      x-patternProperties:
        ^([a-zA-Z_][a-zA-Z0-9_]*)?@(odata|Redfish|Message)\.[a-zA-Z_][a-zA-Z0-9_]*$:
          description: This property shall specify a valid odata or Redfish property.
    StoragePool_v1_8_0_SetEncryptionStateRequestBody:
      additionalProperties: false
      description: This action is used to set the encryption state of the pool.
      properties:
        Enable:
          description: This property indicates the desired encryption state of the
            storage pool.
          type: boolean
          x-longDescription: This property shall indicate the desired encryption state
            of the storage pool.
      required:
      - Enable
      type: object
      x-longDescription: This action shall be used to set the encryption state of
        the storage pool. This may be both a highly impactful, as well as a long running
        operation.
      x-patternProperties:
        ^([a-zA-Z_][a-zA-Z0-9_]*)?@(odata|Redfish|Message)\.[a-zA-Z_][a-zA-Z0-9_]*$:
          description: This property shall specify a valid odata or Redfish property.
    StoragePool_v1_8_0_StoragePool:
      additionalProperties: false
      description: A container of data storage.
      properties:
        '@odata.context':
          $ref: http://redfish.dmtf.org/schemas/v1/odata-v4.yaml#/components/schemas/odata-v4_context
        '@odata.etag':
          $ref: http://redfish.dmtf.org/schemas/v1/odata-v4.yaml#/components/schemas/odata-v4_etag
        '@odata.id':
          $ref: http://redfish.dmtf.org/schemas/v1/odata-v4.yaml#/components/schemas/odata-v4_id
        '@odata.type':
          $ref: http://redfish.dmtf.org/schemas/v1/odata-v4.yaml#/components/schemas/odata-v4_type
        Actions:
          $ref: '#/components/schemas/StoragePool_v1_8_0_Actions'
          description: The available actions for this resource.
          x-longDescription: The Actions property shall contain the available actions
            for this resource.
          x-versionAdded: v1_3_0
        AllocatedPools:
          $ref: http://redfish.dmtf.org/schemas/v1/odata-v4.yaml#/components/schemas/odata-v4_idRef
          description: A reference to the collection of storage pools allocated from
            this storage pool.
          readOnly: true
          x-longDescription: The value of this property shall contain a reference
            to the collection of storage pools allocated from this storage pool.
        AllocatedVolumes:
          $ref: http://redfish.dmtf.org/schemas/v1/odata-v4.yaml#/components/schemas/odata-v4_idRef
          description: A reference to the collection of volumes allocated from this
            storage pool.
          readOnly: true
          x-longDescription: The value of this property shall contain a reference
            to the collection of volumes allocated from this storage pool.
        BlockSizeBytes:
          deprecated: true
          description: Maximum Block size in bytes.
          format: int64
          minimum: 0
          nullable: true
          readOnly: true
          type: integer
          x-deprecatedReason: This property has been Deprecated in favor of StoragePool.v1_1_1.StoragePool.MaxBlockSizeBytes
          x-longDescription: Maximum size in bytes of the blocks which form this Volume.
            If the block size is variable, then the maximum block size in bytes should
            be specified. If the block size is unknown or if a block concept is not
            valid (for example, with Memory), enter a 1.
          x-units: By
        Capacity:
          $ref: http://redfish.dmtf.org/schemas/swordfish/v1/Capacity.yaml#/components/schemas/Capacity_Capacity
          description: Capacity utilization.
          x-longDescription: The value of this property shall provide an information
            about the actual utilization of the capacity within this storage pool.
        CapacitySources:
          description: An array of space allocations to this store.
          items:
            $ref: http://redfish.dmtf.org/schemas/swordfish/v1/Capacity.yaml#/components/schemas/Capacity_CapacitySource
          readOnly: false
          type: array
          x-autoExpand: true
          x-longDescription: Fully or partially consumed storage from a source resource.
            Each entry shall provide capacity allocation data from a named source
            resource.
        CapacitySources@odata.count:
          $ref: http://redfish.dmtf.org/schemas/v1/odata-v4.yaml#/components/schemas/odata-v4_count
        ClassesOfService:
          $ref: http://redfish.dmtf.org/schemas/v1/odata-v4.yaml#/components/schemas/odata-v4_idRef
          description: The ClassesOfService supported by this storage pool.
          readOnly: false
          x-longDescription: This property shall contain references to all classes
            of service supported by this storage pool. Capacity allocated from this
            storage pool shall conform to one of the referenced classes of service.
        Compressed:
          deprecated: true
          description: Indicator of whether or not the StoragePool has compression
            enabled.
          nullable: true
          readOnly: false
          type: boolean
          x-deprecatedReason: This property has been deprecated in favor of the IsCompressed
            and DefaultCompressionBehavior properties.
          x-longDescription: This property shall contain a boolean indicator if the
            StoragePool is currently utilizing compression or not.
          x-versionAdded: v1_3_0
          x-versionDeprecated: v1_6_0
        CompressionEnabled:
          description: Indicates whether or not compression is enabled on the storage
            pool.
          nullable: true
          readOnly: false
          type: boolean
          x-longDescription: The property shall indicate whether or not compression
            is enabled on the storage pool.
          x-versionAdded: v1_6_0
        Deduplicated:
          deprecated: true
          description: Indicator of whether or not the StoragePool has deduplication
            enabled.
          nullable: true
          readOnly: false
          type: boolean
          x-deprecatedReason: This property has been deprecated in favor of the IsDeduplicated
            and DefaultDedupeBehavior properties.
          x-longDescription: This property shall contain a boolean indicator if the
            StoragePool is currently utilizing deduplication or not.
          x-versionAdded: v1_3_0
          x-versionDeprecated: v1_6_0
        DeduplicationEnabled:
          description: Indicates whether or not deduplication is enabled on the storage
            pool.
          nullable: true
          readOnly: false
          type: boolean
          x-longDescription: The property shall indicate whether or not deduplication
            is enabled on the storage pool.
          x-versionAdded: v1_6_0
        DefaultClassOfService:
          $ref: http://redfish.dmtf.org/schemas/v1/odata-v4.yaml#/components/schemas/odata-v4_idRef
          description: The default class of service for entities allocated from this
            storage pool.
          readOnly: false
          x-longDescription: If present, this property shall reference the default
            class of service for entities allocated from this storage pool.  If the
            ClassesOfService collection is not empty, then the value of this property
            shall be one of its entries. If not present, the default class of service
            of the containing StorageService entity shall be used.
          x-versionAdded: v1_2_0
        DefaultCompressionBehavior:
          description: Indicates the default dedupe behavior applied to the child
            resource (E.g., volume or storage pool) created out of the storage pool
            if the 'Compressed' property is not set on the create request.
          nullable: true
          readOnly: false
          type: boolean
          x-longDescription: If implemented, this property shall indicate the default
            dedupe behavior applied to the child resource (E.g., volume or storage
            pool) created out of the storage pool if the 'Compressed' property is
            not set on the create request.
          x-versionAdded: v1_6_0
        DefaultDeduplicationBehavior:
          description: Indicates the default deduplication behavior applied to the
            child resource (E.g., volume or storage pool) created out of the storage
            pool if the 'Deduplicated' property is not set on the create request.
          nullable: true
          readOnly: false
          type: boolean
          x-longDescription: If implemented, this property shall indicate the default
            deduplication behavior applied to the child resource (E.g., volume or
            storage pool) created out of the storage pool if the 'Deduplicated' property
            is not set on the create request.
          x-versionAdded: v1_6_0
        DefaultEncryptionBehavior:
          description: Indicates the default dedupe behavior applied to the child
            resource (E.g., volume or storage pool) created out of the storage pool
            if the 'Encrypted' property is not set on the create request.
          nullable: true
          readOnly: false
          type: boolean
          x-longDescription: If implemented, this property shall indicate the default
            dedupe behavior applied to the child resource (E.g., volume or storage
            pool) created out of the storage pool if the 'Encrypted' property is not
            set on the create request.
          x-versionAdded: v1_6_0
        Description:
          oneOf:
          - $ref: http://redfish.dmtf.org/schemas/v1/Resource.yaml#/components/schemas/Resource_Description
          - enum:
            - null
          readOnly: true
        Encrypted:
          deprecated: true
          description: Indicator of whether or not the StoragePool has encryption
            enabled.
          nullable: true
          readOnly: false
          type: boolean
          x-deprecatedReason: This property has been deprecated in favor of the IsEncrypted
            and DefaultEncryptionBehavior properties.
          x-longDescription: This property shall contain a boolean indicator if the
            StoragePool is currently utilizing encryption or not.
          x-versionAdded: v1_3_0
          x-versionDeprecated: v1_6_0
        EncryptionEnabled:
          description: Indicates whether or not encryption is enabled on the storage
            pool.
          nullable: true
          readOnly: false
          type: boolean
          x-longDescription: The property shall indicate whether or not encryption
            is enabled on the storage pool.
          x-versionAdded: v1_6_0
        IOStatistics:
          $ref: http://redfish.dmtf.org/schemas/swordfish/v1/IOStatistics.yaml#/components/schemas/IOStatistics_IOStatistics
          description: Statistics for this StoragePool.
          x-longDescription: The value shall represent IO statistics for this StoragePool.
          x-versionAdded: v1_2_0
        Id:
          $ref: http://redfish.dmtf.org/schemas/v1/Resource.yaml#/components/schemas/Resource_Id
          readOnly: true
        Identifier:
          $ref: http://redfish.dmtf.org/schemas/v1/Resource.yaml#/components/schemas/Resource_Identifier
          description: The value identifies this resource.
          x-longDescription: The value identifies this resource. The value shall be
            unique within the managed ecosystem.
        Links:
          $ref: '#/components/schemas/StoragePool_v1_8_0_Links'
          description: The links object contains the links to other resources that
            are related to this resource.
          x-longDescription: The Links property, as described by the Redfish Specification,
            shall contain references to resources that are related to, but not contained
            by (subordinate to), this resource.
        LowSpaceWarningThresholdPercents:
          description: Low space warning threshold specified in percents.
          items:
            format: int64
            nullable: true
            type: integer
          maximum: 100
          minimum: 0
          readOnly: false
          type: array
          x-longDescription: 'Each time the following value is less than one of the
            values in the array the LOW_SPACE_THRESHOLD_WARNING event shall be triggered:
            Across all CapacitySources entries, percent = (SUM(AllocatedBytes) - SUM(ConsumedBytes))/SUM(AllocatedBytes).'
          x-units: '%'
        MaxBlockSizeBytes:
          description: Maximum Block size in bytes.
          format: int64
          minimum: 0
          nullable: true
          readOnly: true
          type: integer
          x-longDescription: If present, the value is the maximum block size of an
            allocated resource. If the block size is unknown or if a block concept
            is not valid (for example, with Memory), this property shall be NULL.
          x-units: By
          x-versionAdded: v1_1_1
        NVMeEnduranceGroupProperties:
          description: This property contains properties to use when StoragePool is
            used to describe an NVMe Endurance Group.
          oneOf:
          - $ref: '#/components/schemas/StoragePool_v1_8_0_NVMeEnduranceGroupProperties'
          - enum:
            - null
          x-longDescription: This property shall contain properties to use when StoragePool
            is used to describe an NVMe Endurance Group.
          x-versionAdded: v1_4_0
        NVMeProperties:
          description: NVMe properties for this storage pool.
          oneOf:
          - $ref: '#/components/schemas/StoragePool_v1_8_0_NVMeProperties'
          - enum:
            - null
          x-longDescription: The property shall indicate the type of storage pool.
          x-versionAdded: v1_6_0
        NVMeSetProperties:
          description: This property contains properties to use when StoragePool is
            used to describe an NVMe Set.
          oneOf:
          - $ref: '#/components/schemas/StoragePool_v1_8_0_NVMeSetProperties'
          - enum:
            - null
          x-longDescription: This property shall contain properties to use when StoragePool
            is used to describe an NVMe Set.
          x-versionAdded: v1_4_0
        Name:
          $ref: http://redfish.dmtf.org/schemas/v1/Resource.yaml#/components/schemas/Resource_Name
          readOnly: true
        Oem:
          $ref: http://redfish.dmtf.org/schemas/v1/Resource.yaml#/components/schemas/Resource_Oem
          description: The OEM extension property.
          x-longDescription: This property shall contain the OEM extensions.  All
            values for properties that this object contains shall conform to the Redfish
            Specification-described requirements.
        PoolType:
          deprecated: true
          description: Pool usage type for this storage pool.
          items:
            oneOf:
            - $ref: http://redfish.dmtf.org/schemas/swordfish/v1/StoragePool.yaml#/components/schemas/StoragePool_PoolType
            - enum:
              - null
          readOnly: true
          type: array
          x-deprecatedReason: This property has been deprecated in favor of the SupportedPoolTypes
            property.
          x-longDescription: The property shall indicate the type of storage pool.
          x-versionAdded: v1_6_0
          x-versionDeprecated: v1_7_0
        RecoverableCapacitySourceCount:
          description: Current number of capacity source resources that are available
            as replacements.
          format: int64
          nullable: true
          readOnly: false
          type: integer
          x-longDescription: The value is the number of available capacity source
            resources currently available in the event that an equivalent capacity
            source resource fails.
          x-versionAdded: v1_2_0
        RemainingCapacityPercent:
          description: The percentage of the capacity remaining in the StoragePool.
          format: int64
          nullable: true
          readOnly: true
          type: integer
          x-longDescription: If present, this value shall return  {[(SUM(AllocatedBytes)
            - SUM(ConsumedBytes)]/SUM(AllocatedBytes)}*100 represented as an integer
            value.
          x-versionAdded: v1_1_0
        ReplicationEnabled:
          description: Indicates whether or not replication is enabled on the storage
            pool.
          nullable: true
          readOnly: false
          type: boolean
          x-longDescription: The property shall indicate whether or not replication
            is enabled on the storage pool. If enabled for pool, replication can still
            be disabled on individual resources (e.g., volumes) within the pool.
          x-versionAdded: v1_8_0
        Status:
          $ref: http://redfish.dmtf.org/schemas/v1/Resource.yaml#/components/schemas/Resource_Status
          description: The property contains the status of the StoragePool.
          x-longDescription: The property shall contain the status of the StoragePool.
        SupportedPoolTypes:
          description: A collection of the Pool Types supported by the storage pool.
          items:
            oneOf:
            - $ref: http://redfish.dmtf.org/schemas/swordfish/v1/StoragePool.yaml#/components/schemas/StoragePool_PoolType
            - enum:
              - null
          readOnly: true
          type: array
          x-longDescription: This collection shall contain all the PoolType values
            supported by the storage pool.
          x-versionAdded: v1_7_0
        SupportedProvisioningPolicies:
          description: This collection specifies all supported storage allocation
            properties for the Storage Pool.
          items:
            oneOf:
            - $ref: http://redfish.dmtf.org/schemas/swordfish/v1/DataStorageLoSCapabilities.yaml#/components/schemas/DataStorageLoSCapabilities_ProvisioningPolicy
            - enum:
              - null
          readOnly: false
          type: array
          x-longDescription: This collection shall specify all supported storage allocation
            policies for the Storage Pool.
          x-versionAdded: v1_3_0
        SupportedRAIDTypes:
          description: A collection of the RAID Types supported by the storage pool.
          items:
            oneOf:
            - $ref: http://redfish.dmtf.org/schemas/swordfish/v1/Volume.yaml#/components/schemas/Volume_RAIDType
            - enum:
              - null
          readOnly: true
          type: array
          x-longDescription: This collection shall contain all the RAIDType values
            supported by the storage pool.
          x-versionAdded: v1_3_0
      required:
      - '@odata.id'
      - '@odata.type'
      - Id
      - Name
      type: object
      x-longDescription: A container of data storage capable of providing capacity
        conforming to one of its supported classes of service. The storage pool does
        not support IO to its data storage.
      x-patternProperties:
        ^([a-zA-Z_][a-zA-Z0-9_]*)?@(odata|Redfish|Message)\.[a-zA-Z_][a-zA-Z0-9_]*$:
          description: This property shall specify a valid odata or Redfish property.
title: '#StoragePool.v1_8_0.StoragePool'
x-copyright: Copyright 2015-2026 Storage Networking Industry Association (SNIA), USA.
  All rights reserved.
x-language: en
x-owningEntity: SNIA
x-release: 1.2.5
