emulator:
  url: "http://localhost:8080"
  timeout: 10

auth:
  type: "basic"
  username: "admin"
  password: "admin"

specification:
  version: "1.2.9"

filters:
  resources:
    - "StorageSystems"
    - "StoragePools"
    - "Volumes"
    - "Drives"
